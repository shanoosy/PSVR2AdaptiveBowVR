# PSVR2AdaptiveBowVR — GitHub Actions Build
Upload this folder to a new GitHub repo and the included workflow will build the mod on Windows.
After the run completes, download the artifact zip and install it in MO2.
